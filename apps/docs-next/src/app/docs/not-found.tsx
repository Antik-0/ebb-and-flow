import type { Metadata } from 'next'
import { EbbNotFound } from '#/theme'

export default EbbNotFound

export const metadata: Metadata = {
  title: 'Not Found',
  description: 'The page you are looking for does not exist.'
}
