{
  "name": "ebb-and-flow",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.14.0",
  "scripts": {
    "boot": "pnpm install && pnpm stub",
    "build": "turbo run build --filter=blog",
    "clean": "node ./scripts/cleanup.mjs --del-lock",
    "dev": "turbo run dev --filter=blog",
    "eslint": "eslint --fix --cache --cache-location=.temp/.eslintcache",
    "lint": "pnpm run oxlint && pnpm run eslint",
    "lint:debug": "pnpm dlx @eslint/config-inspector@latest",
    "oxlint": "oxlint --fix --no-ignore docs/.vitepress .",
    "preinstall": "pnpm dlx only-allow pnpm",
    "preview": "turbo run preview --filter=blog",
    "stub": "turbo run stub"
  },
  "lint-staged": {
    "*.{ts,vue}": [
      "oxlint --fix && pnpm run eslint"
    ],
    "*.{json}": [
      "pnpm run eslint"
    ]
  },
  "devDependencies": {
    "@config/eslint": "workspace:*",
    "@config/prettier": "workspace:*",
    "@types/node": "catalog:",
    "eslint": "catalog:",
    "lint-staged": "catalog:",
    "oxlint": "catalog:",
    "picocolors": "catalog:",
    "prettier": "catalog:",
    "turbo": "catalog:",
    "unbuild": "catalog:",
    "unocss": "catalog:"
  }
}
